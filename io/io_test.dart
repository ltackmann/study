
import 'dart:io';

main() {
  group("file test", () {
    setUp(() {
      // delete file if it exists
    });
    
    test("create file", () {
      // assert by checking file exists
    });
    
    test("write to file", () {
      // assert by readin file
    });
    
    tearDown(() {
      // delete file
    });
  });
}
