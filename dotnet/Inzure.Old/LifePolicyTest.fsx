// changing a policy back in time 

// creating a policy at time t and changing it at time t+1 causes it to be viewed at time t+1 in its changed state and at time t in its initial state

// canceling a event at time t+1 causes it to be viewed as its original state (but the cancelled event still exists)