package org.randompage.samples.seam.wiki.test;

import org.randompage.samples.seam.wiki.test.utils.WebTester;
import org.testng.annotations.Test;

// TODO switch to Selenium/htmlunit
public class LoginTest extends WebTester {
	@Test(description = "should login as newly created user")
	public void loginTest() throws Exception {
		throw new AssertionError("TODO");
	}
}
